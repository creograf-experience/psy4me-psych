import styled from 'styled-components';
import {
  verticalScale,
} from 'react-native-size-matters';

export const AvatarWrapper = styled.View`
  justify-content: center;
  align-items: center;
  margin-vertical: ${verticalScale(30)};
`;
