import styled from 'styled-components';

import { verticalScale } from 'react-native-size-matters';


export const DotsButtonWrapper = styled.TouchableOpacity`
  padding-bottom: ${verticalScale(7)};
`;
