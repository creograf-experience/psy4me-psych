import styled from 'styled-components';
import { scale } from 'react-native-size-matters';


export const DocumentFieldWrapper = styled.View`
  height: ${scale(180)};
  width: 90%;
  align-self: center;
  margin-vertical: ${scale(15)}
`;
