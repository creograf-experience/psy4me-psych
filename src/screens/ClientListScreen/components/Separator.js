import styled from "styled-components";

import { colors } from "../../../constants";

export const Separator = styled.View`
  width: 100%;
  height: 5;
  margin: 0 auto;
  background-color: ${colors.halfTransparentColorPrimary};
`;