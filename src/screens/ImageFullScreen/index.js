import { connect } from 'react-redux';
import ImageFullScreenComponent from './ImageFullScreen';


const mapStateToProps = state => ({
  
});

const mapDispatchToProps = dispatch => ({
 
});

export const ImageFullScreen = connect(
  mapStateToProps,
  mapDispatchToProps
)(ImageFullScreenComponent);